// Generated by ts-to-zod
import { z } from 'zod';

const realmSchema = z.object({
  id: z.number(),
  name: z.null(),
  slug: z.string(),
});

const responseBaseSchema = z.any();

const factionsSchema = z.any();

const originsSchema = z.any();

const nameIdSchema = z.any();

const keyBaseSchema = z.any();

const journalInstanceSchema = keyBaseSchema.extend({
  id: z.number(),
  name: z.null(),
});

const guildSchema = nameIdSchema.extend({
  realm: realmSchema,
});

const entrySchema = z.object({
  faction: z.object({
    type: factionsSchema,
  }),
  guild: guildSchema,
  rank: z.number(),
  region: z.union([z.literal('cn'), originsSchema]),
  timestamp: z.number(),
});

/**
 * The response for a Mythic Raid leaderboard.
 * @see {@link https://develop.battle.net/documentation/world-of-warcraft/game-data-apis}
 */
export const mythicRaidLeaderboardResponseSchema = responseBaseSchema.extend({
  criteria_type: z.string(),
  entries: z.array(entrySchema),
  journal_instance: journalInstanceSchema,
  slug: z.string(),
});
